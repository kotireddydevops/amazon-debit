package ${artifactPackage};

public final class ${infoObjectName} {
  public static final String Version = "${project.version}";

  private ${infoObjectName}() {
    throw new UnsupportedOperationException();
  }
}
